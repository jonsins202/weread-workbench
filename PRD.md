# 微信读书笔记工作台 · PRD v1.0

> 状态：定稿（2026-08-24）
> 说明：本文档为开发前冻结的产品需求。执行时按里程碑推进，规范变更需先更新本文档。

---

## 1. 定位

本地 Web 应用。双管理员模型：

- **应用 = 档案管理员 + 阅读工作台**：建档（同步生成）、编号（命名规范 + bookId）、归档（attachments）、更新（重新同步）、管理操作（重命名/删除）、可视化阅读、编辑、AI 延伸。
- **Obsidian = 阅览室**：自由批注、双链、图谱、插件生态。

**.md 文件是唯一数据源**，应用是 Obsidian 库的「另一个窗口」——随时可以只用 Obsidian 而不损失任何东西。

## 2. 背景与问题

- treeboat 方案已弃用（可视化弱、想法内容缺失、格式不合意，产物已删除）。
- 手工「问 Claude → 复制成 callout」流程低效，需自动化。
- Obsidian 内做定制可视化开发不便。

## 3. 用户与场景（单用户本地应用）

| 场景 | 描述 |
|---|---|
| S1 同步 | 读完一本书/一期杂志 → 打开应用一键同步最新划线与想法 |
| S2 阅读 | 章节导航、划线卡片、图片的可视化浏览 |
| S3 深挖 | 选中某条划线 → 与 Claude 对话 → 满意后一键插入 callout |
| S4 编辑 | 改错字、补充自己的思考、把图片插入到笔记指定位置 |
| S5 回顾 | 书架总览与搜索 |
| S6 管理 | 重命名、删除、查看同步状态 |

## 4. 功能需求

### F1 同步器（M1）

- 直调 weread 网关（`i.weread.qq.com/api/agent/gateway`，鉴权用环境变量 `WEREAD_API_KEY`）。
- 全量回填：`/user/notebooks`（lastSort 游标分页）遍历所有笔记本 → 对每本书调 `/book/bookmarklist`（划线）+ `/review/list/mine`（想法）合并。
- 生成/更新 `微信读书笔记/{书名}_读书笔记.md`；增量判断依据 frontmatter `synced_at` + `bookId`。
- 幂等：重复同步不产生重复块。
- 只在应用自管目录内写文件。

### F2 书架页 = 管理面板（M2）

- 封面卡片：书名、阅读进度、划线/想法数、最近同步时间。
- 搜索、排序。
- 应用内重命名（只改文件名，不动 bookId）。
- 删除：物理删除 + 二次确认。
- 同步状态徽标（有新增划线时提示）。
- 手动笔记标识（无 bookId 的共管文件）。

### F3 笔记页（M2）

- 章节锚点导航。
- 划线卡片分层展示：原文 → 图片 → 💭 我的思考 → AI callout。
- weread 深链按钮（跳书 / 跳章节）。
- 单书同步按钮。

### F4 编辑（M3）

- 块级增删改（划线文本、想法、callout）。
- 图片插入到任意指定位置。
- 保存前重读磁盘、按内容重定位目标块。
- 「解析 → 原样写回 → 字节一致」作为单元测试。

### F5 AI 延伸（M4）

- 右侧对话面板：选中划线后发起对话。
- 后端 spawn `claude -p`（非交互），上下文 = 书名 / 章节 / 选中划线 ± 相邻划线。
- 多轮对话；插入前预览；确认后追加 callout。
- 历史内容永不修改。Adapter 接口预留其它 CLI（如 codex）。

### F6 二期 backlog

- 旧手写笔记（`微信读书读书笔记_财经杂志/`）导入与合并。
- 阅读统计视图。
- codex 适配器。
- 全库搜索。

## 5. 文件规范（已冻结）

### 5.1 目录（应用自管，平铺）

```
{vault}/微信读书笔记/
  ├─ {书名}_读书笔记.md
  └─ attachments/        ← 全部图片附件
```

建议将 Obsidian「新附件默认存放位置」指向 `微信读书笔记/attachments`，使手动贴图与应用行为一致。

### 5.2 命名

- 杂志（标题含「年第N期」）：紧凑式 → `财经2026第18期_读书笔记.md`
- 普通书：完整 API 书名（替换文件名非法字符，超 80 字截断）→ `牧羊少年奇幻之旅_读书笔记.md`
- 图片：`{书名主名}_{YYYYMMDD_HHmm}.png`，笔记内用 `![[完整文件名]]` 引用（时间戳保证全局唯一，wikilink 不歧义）。
- frontmatter 存完整 `title` + `bookId`，文件名不承担唯一性职责。

### 5.3 笔记结构（与手写笔记同构）

```
---
bookId: "3300207184"
title: "财经（2026年第11期）"        # 完整 API 书名
author: "财经"
cover: "https://..."
status: 在读                          # 在读 / 读完
tags: [读书笔记, 微信读书]
source: 微信读书
created: 2026-08-24
synced_at: 2026-08-24 15:30
---

# 财经（2026年第11期）— 读书笔记

> 阅读时间：2026年8月 ｜ 打开阅读：[weread://reading?bId=...](weread://reading?bId=...)

## 一、章节名

[跳转章节](weread://reading?bId=...&chapterUid=3) ｜ 21 条划线 · 4 条想法

### 划线 1

> 划线原文……

![[财经2026第11期_20260824_1530.png]]

💭 **我的思考：** ……

> [!example] 🤖 AI 分析（Claude · 2026.08.24）
> **问：** ……
>
> **答：** ……

### 💭 仅想法（无对应划线）

💭 **我的思考：** ……
```

### 5.4 AI 插入格式

```markdown
> [!example] 🤖 AI 分析（Claude · YYYY.MM.DD）
> **问：** 用户的问题
>
> **答：** AI 回答正文
```

- 每轮问答一个 callout，只追加不修改历史。
- 用户自己的思考用 `💭 **我的思考：**`。
- AI 主动提出的延伸问题用 `> [!tip] 🧠 延伸思考`。

### 5.5 Obsidian 兼容铁律

- 只用 Obsidian 原生语法（callout / `[[双链]]` / `![[图片]]` / YAML frontmatter / 标准 Markdown）。
- 不埋 HTML 注释标记，不整文件重排版。
- 保存只精准替换目标块，其余内容逐字保留。

## 6. 管理策略（已确认）

| 策略 | 规则 |
|---|---|
| **身份锚点** | frontmatter `bookId` 是文件唯一身份。同步按 bookId 定位目标文件——用户在 Obsidian 手动改名也不会产生重复文件。 |
| **重命名** | 应用内操作，只改文件名不动 bookId。 |
| **移出目录** | 应用检测到某书笔记不在自管目录 → 弹提示「是否重新创建」；**绝不扫描/修改自管目录之外的任何文件**。 |
| **手动文件** | 无 bookId 的共管文件：书架页正常显示、可编辑、可删除（同样二次确认）；永不参与同步、永不被同步覆盖。 |
| **删除** | 物理删除 + 二次确认；连带删除 attachments 中以该书名前缀命名的图片；git 为最终兜底。 |

## 7. 非功能需求

- **无损往返**：parse → write 无改动时字节一致（单元测试固化）。
- **并发安全**：保存前重读磁盘重定位；检测到外部修改自动重解析。
- **原子写入**：临时文件 + rename。
- **无数据库**：全部状态在 .md 与 frontmatter 中。
- **密钥安全**：API key 只走环境变量 `WEREAD_API_KEY`，不入库不入配置文件。

## 8. 技术方案（已定）

| 项 | 选型 |
|---|---|
| 后端 | Node.js + Express |
| 前端 | Vite + React + TypeScript |
| 数据源 | weread 网关直调（skill_version ≥ 1.0.3） |
| AI | claude CLI（`claude -p` 非交互，spawn） |
| 项目位置 | `K:\weread-workbench`（vault 外） |
| 启动 | `启动读书工作台.bat` 双击启动并自动开浏览器 |

## 9. 里程碑

1. **M1 同步器**：文件规范落地 + 全量回填（真库验证）。
2. **M2 书架管理面板 + 笔记页**：可视化浏览 + 管理操作。
3. **M3 编辑**：块级 CRUD + 无损写回。
4. **M4 AI**：claude 对话 + callout 插入闭环。

每个里程碑在真实库上验证通过后进入下一个。

## 10. 边界（不做什么）

- 不修改自管目录之外的任何 vault 文件。
- 不做移动端 / 多用户 / 云同步。
- 不替代 Obsidian 的双链编辑与图谱。
- 不代理微信读书以外的数据源。

---

## 11. 二期功能（v1.1 增补 · 2026-08-24 讨论定稿）

> 范围调整：F6 backlog 中「codex 适配器」暂不实施（Adapter 架构已预留，config 可切）；「阅读统计视图」暂缓（待明确想要的统计维度再设计）。

### F7 旧手写笔记导入合并

**场景**：把库内其它目录的旧笔记（首批：`微信读书读书笔记_财经杂志/` 两份）中的手写批注，并入自管目录的对应笔记，统一由应用管理。

**流程**：

1. 书架页新增「导入旧笔记」入口：选择 vault 内的旧笔记文件（只允许 vault 路径内，只读打开）。
2. 解析旧笔记 → 每条旧划线按内容锚点（quoteKey 归一化）匹配自管笔记中的条目 → 输出**匹配报告**：自动匹配 N 条（明细）+ 无法匹配 M 条（原因：目标笔记无此划线 / 目标书未同步）。
3. 用户确认报告后执行合并：
   - **匹配成功**：旧划线下的批注段（💭思考 / AI callout / 其它文本）通过 M3 编辑引擎逐条挂到新笔记对应条目末尾。
   - **图片**：旧笔记引用的图片复制进 `attachments/` 按规范重命名，引用同步改写；源文件缺失则保留原引用并在报告中列出。
   - **无法匹配**：以「原文引用（>）+ 批注内容」形式追加到「仅想法」区，上下文不丢。
4. **幂等**：重复导入按内容去重（同条目下同内容段跳过），不产生重复批注。
5. **旧文件只读不动**（§10 边界）；导入完成后提示「原文件未改动，可自行归档或删除」。
6. 若目标书还不在自管目录 → 提示「请先同步该书」并中止，不做半份合并。

**验收标准**：第 10/11 期旧笔记导入后——所有 💭思考 / callout / 图片出现在新笔记对应位置；二次导入 0 新增；新旧内容并存无重复；git diff 显示仅自管目录有变化。

### F8 全库搜索

- 书架页搜索框升级为**全局搜索**：范围 = 书名 / 作者 / 章节标题 / 划线原文 / 💭想法与思考 / callout 正文，跨全部笔记。
- 结果列表项：书名 · 章节 · 类型徽标（划线/想法/思考/批注/书名）· 命中片段（关键词高亮，前后各约 30 字）。
- 点击结果 → 打开笔记页并平滑滚动定位到对应条目。
- 实现：后端 `GET /api/search?q=` 遍历自管目录实时解析匹配（当前书量级无需索引）；大小写不敏感；空格分词按 AND 组合。
- 边界：只搜自管目录；不做全文索引 / 正则 / 拼音。

### F9 同段共鸣（v1.2 增补 · 2026-08-24）

**定义**：在笔记页引入「其他读者」的视角——同一段落有多少人划过、挂了哪些公开想法，把独白式笔记变成对话式笔记。

- **数据链路**：`/book/bestbookmarks`（全书热门划线 TOP20，带人数+原文）→ 按 chapterUid 分组调 `/book/readreviews`（热门段下的公开想法，每段取前 5 条）。
- **归属匹配**：热门段 markText 与笔记条目按 quoteKey 文本指纹匹配（精确相等或互为前缀、重叠≥12 字）；range 在不同接口间会漂移，禁止按 range 匹配。
- **UI**：命中的条目在原文下方显示橙色徽标「🔥 N 人划过这段 · M 条想法」，点击展开想法面板（作者/日期/内容）；笔记底部折叠区「本书热门划线 · 你没划过的 N 条」展示未命中的热门段（只读）。
- **引入落盘**：想法面板每条带「引入」按钮，两步确认后以 `> [!quote] 💬 读者共鸣 · {作者}（{日期}）` callout 追加到该条目下；已引入的自动识别并禁用按钮；同步永不丢失、永不自动写入。
- **AI 联动**：对命中条目提问时，自动把该段公开想法（最多 3 条）注入 AI 上下文作对照参考；AI 面板显示「已附 N 条读者想法」提示。
- **缓存**：原始数据按 bookId 内存 + `.cache/social-{bookId}.json` 双缓存，24 小时过期；匹配每次请求实时算。
- **边界**：公众内容默认只在 UI 展示，绝不自动写进 .md；进口书/网文（CB_ 等 bookId）官方无热门划线数据，接口返回空时 UI 静默不显示。
