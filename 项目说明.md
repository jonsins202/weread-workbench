# 微信读书笔记工作台 · 项目说明

> 版本：v1.1（2026-08-24）｜ 配套文档：[PRD.md](./PRD.md)（产品需求）· [README.md](./README.md)（快速上手）
> 本文是项目的完整技术说明：架构、机制、规范、踩坑记录与维护指南。

---

## 1. 这是什么

一个本地 Web 应用，把**微信读书的划线、想法、手写笔记**同步为 **Obsidian 原生 Markdown**，并提供可视化书架、笔记阅读、块级编辑、AI 对话延伸、旧笔记导入和全库搜索。

核心设计哲学（「双管理员模型」）：

- **应用 = 档案管理员 + 阅读工作台**：建档（同步）、编号（bookId）、归档（attachments）、更新、管理操作、可视化、编辑、AI
- **Obsidian = 阅览室**：自由批注、双链、图谱、插件生态
- **.md 文件是唯一数据源**：无数据库；应用只是库的「另一个窗口」，随时可以只用 Obsidian 而无损

## 2. 快速开始

```bash
# 环境要求：Node.js >= 20；环境变量 WEREAD_API_KEY（微信读书 App → 设置 → 微信读书 Skill 获取，wrk- 开头）
# 配置：config.json 的 vaultRoot 指向 Obsidian 库根目录

npm install               # 首次
npm run build             # 构建前端（或直接双击 启动读书工作台.bat 自动完成）
npm start                 # 启动 → 浏览器打开 http://localhost:5175

npm run sync              # 命令行同步（不开界面）
npm run sync -- --dry-run # 只看计划不写文件
npm run sync -- --book 牧羊少年  # 单书同步
npm test                  # 单元测试（node --test，28 个用例）
```

日常使用：双击 `启动读书工作台.bat`。

## 3. 架构与数据流

```
微信读书网关 API ──► server/weread.js ──► server/sync.js（同步编排）
                                          │  ├─ server/naming.js   命名/归一化
                                          │  ├─ server/template.js 渲染笔记（幂等）
                                          │  └─ server/merger.js   解析旧内容（保全用户批注）
                                          ▼
                          K:/obsidian_cangku/微信读书笔记/*.md  ◄─── server/writer.js（块级编辑）
                                          ▲                              ▲
浏览器 (React+TS) ◄──► server/index.js（Express，REST API + 静态托管）──┤
                        ├─ /api/notes*      书架/笔记/重命名/删除/编辑/图片
                        ├─ /api/sync*       同步与状态徽标
                        ├─ /api/agent/chat  AI 对话（spawn claude -p）
                        ├─ /api/import/*    旧笔记导入
                        ├─ /api/search      全库搜索
                        ├─ /api/stats/heatmap 阅读热力图（server/stats.js）
                        ├─ /api/notes/:f/resonance 同段共鸣（server/social.js）
                        └─ /api/attachments 图片服务
```

一次同步的数据流：`/user/notebooks`（全量书单，lastSort 游标翻页）→ 每本书 `/book/bookmarklist`（划线）+ `/review/list/mine`（想法/手写）→ 想法按 range 挂回划线（±2 容差）→ 手写笔记图片下载进 attachments → 渲染笔记 → 与磁盘旧文件按内容比对（幂等）→ 有变化才原子写回。

## 4. 目录结构与模块职责

```
weread-workbench/
├── PRD.md / README.md / 项目说明.md
├── config.json            # 唯一配置文件
├── 启动读书工作台.bat
├── server/
│   ├── config.js          # 配置加载；vaultRoot 支持 WEREAD_VAULT_ROOT 环境变量覆盖（测试用）
│   ├── weread.js          # 网关客户端（分页/容错已踩坑适配）
│   ├── naming.js          # 文件名规范（杂志紧凑式/完整书名/图片命名/中文数字/quoteKey 归一化）
│   ├── merger.js          # 解析既有笔记：按划线内容键提取用户批注（重同步保全的核心）
│   ├── template.js        # 笔记渲染器：确定性排序、全局想法/图片去重、隐藏清单过滤
│   ├── sync.js            # 同步编排：bookId 锚点定位、幂等比较、原子写、孤儿/手动文件识别
│   ├── notes.js           # 库操作：书架列表、结构化解析（UI 视图）、重命名、删除
│   ├── writer.js          # 块级编辑引擎：内容锚点定位、六种操作、frontmatter 隐藏清单、图片落盘
│   ├── agent.js           # AI Adapter（claude 默认，预留 codex）+ 上下文/prompt 构建
│   ├── chats.js           # AI 对话持久化：按 书+条目 存 .cache/chats/，跨刷新续接
│   ├── import.js          # 旧笔记导入：划线式匹配挂载 + 随笔式整节导入，幂等去重
│   ├── stats.js           # 阅读热力图：逐月拉取 /readdata/detail 合并，双缓存 30 分钟
│   ├── social.js          # 同段共鸣：热门划线归属 + 公开想法，文本指纹匹配，双缓存 24h
│   ├── index.js           # Express 入口（API + 前端静态托管）
│   └── cli.js             # 命令行入口
├── web/                   # Vite + React + TS 前端
│   └── src/pages/         # Shelf（书架/搜索/导入/热力图）· NoteView（笔记/编辑/AI 面板/共鸣）
└── test/                  # 39 个单元测试：命名/渲染幂等/编辑保全/隐藏复活/导入/热力图/共鸣匹配
```

## 5. 文件规范（冻结于 PRD §5）

```
{vault}/微信读书笔记/                  # 应用唯一自管目录
├── {书名}_读书笔记.md                 # 平铺；杂志用紧凑式（财经2026第18期_读书笔记.md）
└── attachments/                       # 全部图片（含手写笔记图、导入图、应用内上传图）
```

**笔记结构**（与用户手写笔记同构）：

```
---                                      # frontmatter（应用管理的元数据区）
bookId / title / author / cover / status / progress / tags / source / created / synced_at
hidden_keys: […]                         # 应用内删除过的条目（同步不复活）
hidden_ideas: […]                        # 应用内删除/编辑过的想法（同步不复活）
---
# {书名} — 读书笔记
> 阅读时间：… ｜ 进度：…% ｜ 打开阅读：weread://…
## 一、章节名
[跳转章节](weread://…) ｜ N 条划线 · M 条想法
### 划线 1
> 原文…
💭 **我的想法：** …                      # 来自微信读书的想法（服务端内容）
💭 **我的思考：** …                      # 用户手工添加（永不被动）
> [!example] 🤖 AI 分析（Claude · 日期）  # AI 插入（含问/答，只追加）
### 💭 想法 N                            # 无宿主划线的想法（原文摘录做引用）
### 💭 仅想法（无对应划线）               # 完全独立的内容区（导入随笔也落这里）
```

**Obsidian 兼容铁律**：只用原生语法（callout / `[[双链]]` / `![[图片]]` / YAML）；不埋 HTML 注释锚点；不整文件重排版；保存只精准替换目标块。

## 6. 核心机制详解

### 6.1 同步的幂等与内容保全

- **身份锚点**：frontmatter `bookId` 是文件唯一身份，同步按它定位——在 Obsidian 里手动改名不会产生重复文件
- **内容键（quoteKey）**：划线原文去空白归一化取前 80 字。重同步时，旧文件里每条划线下的用户批注按此键挂回新渲染结果；想法/手写图片做**全局去重**，永不重复插入
- **字节级幂等**：用旧 synced_at 渲染后与磁盘逐字节比较，无变化不写盘（`[无变化]`）；有变化才刷新 synced_at 原子写回
- **失败回滚**：拉取或渲染失败保留旧数据不动

### 6.2 防复活（隐藏清单）

应用内删除「服务端内容」（划线条目/想法）时，删除的不只是文本——同时把内容键写入 frontmatter 的 `hidden_keys` / `hidden_ideas`。同步器渲染时会跳过清单里的条目。编辑「服务端想法」= 原想法入清单 + 编辑稿转为「我的思考」永久保留；被隐藏想法的**条目壳**（原文引用）保留，挂在下面的用户内容不丢。

### 6.3 块级编辑（writer.js）

每次操作独立执行「重读磁盘 → 按内容锚点定位 → 行级手术替换 → 原子写回」：Obsidian 同时改动不冲突（锚点自动适应），锚点丢失时报错（409）而非盲写。支持：段落增删改、图片插入任意位置、条目删除（联动隐藏清单）、手动笔记改原文。

### 6.4 AI 延伸（agent.js + chats.js）

`claude -p`（prompt 走 stdin 防命令行长度限制），上下文 = 书名/章节/当前条目 ± 相邻条目 + 同段公众想法（F9）+ 对话历史。回答确认后按 PRD 5.4 格式追加为 callout（`🤖 AI 分析` / `🧠 延伸思考` 两型），只追加不改历史，同步永不丢失。实测简单问题 12~21s，复杂问题约 2 分钟。`config.json` 的 `agent.cli` 可切 codex（Adapter 已预留）。

**对话历史持久化**（chats.js）：服务端是历史的唯一持有者——`/api/agent/chat` 每次自行加载该条目（`bookId+itemKey`）的存量对话→按 `agent.historyRounds`（默认 20 轮）截断后拼进 prompt→**拿到回答才追加本轮**（失败不留幽灵轮次）→原子写回 `.cache/chats/`。效果：同一条划线重开面板/刷新页面都「接着上次」（面板顶部显示已有轮数），换条目即新对话；「清空重开」按钮两步确认后删档重来。每轮重新注入格式指令，长对话不跑版。

### 6.5 旧笔记导入（import.js）

两种旧格式自动识别：**划线式**（`### 划线 N`，按内容键挂回对应条目，对不上的带原文进「仅想法」，纯原文无批注的跳过）；**随笔式**（`### 思考 N`，若含「原文摘录」且能匹配条目则挂到条目下并丢弃重复原文，否则整节转粗体进「仅想法」）。先分析出报告（匹配/转仅想法/随笔/图片/去重明细），确认后执行；图片复制为 `{书名}_导入_{原名}`（确定性命名）；重复导入自动去重；**旧文件永远只读不动**。

注意：旧笔记手工选段与服务器原文常是「同文异段」（文本不同），精确匹配可能挂不上——这是数据本身的差异，不是缺陷；匹配不上的留在「仅想法」区同样安全可读。

### 6.6 全库搜索

`GET /api/search?q=` 实时遍历解析全部笔记（当前量级无需索引），范围覆盖书名/作者/章节/划线/想法/思考/批注，空格分词 AND；结果点击直达笔记页对应条目。

### 6.7 阅读热力图（stats.js）

书架页顶部的 GitHub 风格格子图：最近一年（53 周）每日阅读时长，绿色四档强度（20/50/100 分钟分界），悬浮显示当天分钟数；汇总行 = 近一年总时长 / 阅读天数 / 单日最长 / 活跃日均。

数据链路：`/readdata/detail` 只有 `type=monthly` 才给逐日明细（`readTimes` 的 key 是当天任意时间戳），且只能查单月 → 服务端滚动取 12 个月（`baseTime` 取每月 15 号正午，保证落在目标月内），按 **UTC+8 转日期**后合并去重。缓存双层：内存 30 分钟 + `.cache/heatmap.json` 落盘；`GET /api/stats/heatmap?force=1` 强制刷新。已用 annually 口径交叉验证逐月求和一致（2026-01：29328 秒精确吻合）。

### 6.8 同段共鸣（social.js）

笔记页的「其他读者视角」：`/book/bestbookmarks` 拿全书热门划线 TOP20（人数+原文），按 chapterUid 分组调 `/book/readreviews` 拿热门段下的公开想法（每段前 5 条）；热门段与我的条目按 **quoteKey 文本指纹**匹配（精确或互为前缀且重叠≥12 字——range 在不同接口间会漂移，绝不能按 range 匹配）。命中条目显示「🔥 N 人划过」徽标 + 想法面板；未命中的进底部「你没划过的热门划线」折叠区（只读）。

三条铁律：**公众内容默认只在 UI 展示**，点「引入」（两步内联确认，不用 window.confirm——嵌入式浏览器不弹原生框）才以 `> [!quote] 💬 读者共鸣 · {作者}（{日期}）` callout 落盘，已引入的自动禁用；**同步永不碰这些 callout**（挂条目下的 extras 与 AI callout 同机制保全，实测验证）；**AI 联动**——对命中条目提问时自动注入该段公开想法（≤3 条）作对照参考。缓存：原始数据按 bookId 内存 + `.cache/social-{bookId}.json`，24 小时；匹配每次实时算（笔记可能刚被编辑）。

## 7. 配置说明（config.json）

| 字段 | 默认 | 说明 |
|---|---|---|
| `vaultRoot` | — | Obsidian 库根目录（必填）。支持相对路径，按**项目根**解析——多设备约定写 `../obsidian_cangku`（库克隆在工具旁边），也兼容绝对路径 |
| `notesRelDir` | 微信读书笔记 | 自管目录（相对 vault） |
| `attachmentsRelDir` | attachments | 图片目录（相对自管目录） |
| `port` | 5175 | Web 端口 |
| `gateway` / `skillVersion` | 官方网关 | 一般不动 |
| `requestDelayMs` | 200 | API 请求间隔（礼貌限速） |
| `agent.cli` | claude | AI 后端（claude / codex） |
| `agent.timeoutMs` | 180000 | AI 响应超时 |
| `agent.historyRounds` | 20 | 对话历史注入上限（轮，一问一答=1 轮） |

环境变量：`WEREAD_API_KEY`（必需，兜底解析 `~/.bashrc`）；`WEREAD_VAULT_ROOT`（测试沙盒覆盖）。

## 8. 设计边界（不可越）

1. 只读写 `微信读书笔记/` 自管目录，库内其它文件一概不碰（导入旧笔记也只读）
2. 不做移动端/多用户/云同步；不替代 Obsidian 的双链与图谱
3. 同步笔记的划线原文由微信读书管理（应用内不可改，防同步互相覆盖）；手动笔记完全自由

## 9. 踩坑记录（weread 网关 & 工程实战）

1. `/review/list/mine` 的 `hasMore` 不可靠（=0 时可能还有数据）；`count` 调大到 50 可单页拉全
2. 想法 `range` 末位与划线可能差 1（`17104-17303` vs `17104-17304`）→ 匹配需 ±2 容差
3. `markedStatus==4` 才是读完（官方文档写 1=读完，与实测不符）
4. 划线/想法接口返回顺序不稳定 → 渲染前必须显式排序，否则永远判「有变化」
5. 手写笔记藏在 `pencilNote.imageUrl`（content 为空），需下载图片单独渲染
6. 「标题后空行」解析坑出现过三次（merger/notes/import 各一次）：引用收集必须容忍空行
7. callout 分类正则的 `$` 锚在多行段上永不匹配 → 只对首行做判断
8. Windows 下 Git Bash 的 curl 发中文 JSON body 是 GBK 编码 → 文件名清洗需拒绝乱码字符；测试 API 用 node fetch
9. 同一想法可能同时有「挂靠划线」和「原文摘录条目」两种归宿，位置可能在两次请求间漂移 → 全局内容去重防累积
10. `/readdata/detail` 三种口径的桶粒度不同（monthly=逐日 / annually=逐月 / overall=逐年），逐日明细只有 monthly 模式返回；`readTimes` 的 key 是当天内任意时间戳而非零点，必须经 UTC+8 归一为日期再合并，否则跨天错位
11. 社交接口三坑：`/book/underlines` 的 range 与 bookmarklist 的 range **同段不同值**（开头相同结尾漂移），跨接口匹配必须走文本指纹而非 range；`/book/readreviews` 的 chapterUid 参数很宽松（传错也能跨章返回正确结果），但按章分组调用最稳；进口书/网文（`CB_` 等 bookId）官方无热门划线数据，bestbookmarks 返回空对象属正常，UI 需静默降级

## 10. 测试与质量

`npm test`（node --test，45 用例）：命名规则、渲染结构、**字节级幂等往返**、用户内容保全（思考/图片/AI callout 重同步不丢）、新增划线编号顺延、隐藏清单防复活（含想法条目壳保留回归）、编辑操作全套、外部修改重定位、锚点丢失拒写、导入的匹配/落位/幂等/越界拒绝、热力图日期转换与逐月合并、共鸣文本匹配（精确/前缀/防误配/去重）与 AI 想法注入、对话持久化的存取/隔离/清空/损坏兜底。

真实数据验证记录：20 本书全量回填；连续同步 20/20 无变化；第 10 期导入后同步内容零丢失（两次实测）；书架/笔记页/AI 闭环/搜索跳转/热力图渲染均通过浏览器实测；同段共鸣在《牧羊少年奇幻之旅》实测——9282 人热段徽标、5 条想法展开、两步引入落盘（`> [!quote] 💬 读者共鸣` callout）、同步后保留、重复引入禁用。

## 11. 常见维护问题

| 问题 | 处理 |
|---|---|
| 提示 WEREAD_API_KEY 缺失 | 手机微信读书重新获取（key 会过期），更新环境变量或 `~/.bashrc` |
| 想换一个 Obsidian 库 | 改 `config.json` 的 `vaultRoot`，跑 `npm run sync` 全量回填 |
| 某本书笔记乱了 | 书架里删除该笔记 → 重新同步（会从服务器完整重建；应用内加的批注会随删除丢掉，先手动备份有价值的部分） |
| 想改笔记生成格式 | 改 `server/template.js`（模板）+ 同步跑两遍确认幂等；改完 `npm test` |
| AI 一直超时 | 检查 `claude --version` 可用；复杂问题本身要 2 分钟，可调大 `agent.timeoutMs` |
| 前端改了没生效 | `npm run build` 重建（开发模式用 `npm run dev` + `dev:web` 热更新） |

## 12. 多设备同步（GitHub 双仓库约定）

系统里只有三类东西，分而治之：**工具**（本仓库，代码）、**数据**（Obsidian 库仓库，笔记 + attachments）、**设备私有状态**（`config.json` / `WEREAD_API_KEY` / `.cache/`，不跨设备）。

- **目录约定**：两个仓库克隆到同一父目录并排放置；`config.json` 的 `vaultRoot` 写相对路径 `../obsidian_cangku`（config.js 按项目根解析），于是换设备无需改任何路径配置
- **不进仓库**：`node_modules/`、`web/dist/`（自愈安装）、`config.json`（路径因设备而异）、`.cache/`（可再生缓存；想让 AI 对话历史跨设备续接，在 .gitignore 放行 `.cache/chats/`）
- **同步纪律**：开工前两个仓库各 `git pull`，收工后各 `git push`；同一时间段尽量单设备编辑
- **冲突天然少**：划线/想法由微信读书服务器托管，任何设备重新同步即确定性收敛；git 真正运输的只有用户创作层（💭思考/AI callout/引入的读者共鸣/导入内容），全部小体积纯文本
- **新设备流程**：克隆两仓库并排 → 配 `WEREAD_API_KEY`（~/.bashrc）→ 双击 `启动读书工作台.bat`（自动装依赖、构建、从模板生成 config）

## 13. 后续可扩展方向（backlog）

- 阅读统计扩展维度（月度趋势曲线/划线热词/进度总览；热力图已上线，数据源 `/readdata/detail`）
- Windows 计划任务每日自动同步
- 笔记内 `[[双链]]` 渲染与跳转；应用内改 status/标签；AI 回答流式输出
- 导入的模糊匹配人工确认界面（同段异选的半自动挂载）
